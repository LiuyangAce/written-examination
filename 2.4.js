// 搜索多个文件夹路径（字符串数组，例如['/usr/bin', '/etc/config']），
// 查找是否有公共路径，若有则返回公共路径（字符串），否则返回 null

let arr = ['/usr/bin', '/etc/config']

function handle(arr) {
  let [str1, str2] = arr
  console.log(str1, str2);
  
  let length = str1.length > str2.length ? str2.length : str1.length

  for (let index = 0; index < array.length; index++) {
    const element = str1[index];
    
  }

}

handle(arr)
